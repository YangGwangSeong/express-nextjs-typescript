## backend

```bash
$ npm init
$ npx tsc --init

$ npm install morgan nodemon express --save
$ npm install typescript ts-node
$ npm install @types/node @types/express @types/morgan --save-dev
$ npm install pg typeorm reflect-metadata --save

$ npx typeorm init
```
