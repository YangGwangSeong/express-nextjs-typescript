## install library

## forntend

```bash
$ tailwind css
$ npm install classnames
$ npm install axios
$ npm i swr
$ npm i dayjs                        //시간 라이브러리
$ npm i react-icons                  //아이콘 라이브러리
$ npm install env-cmd                //환경에 따른 env파일 생성
$ npm install sharp                  //운영환경 이미지 최적화 모듈 (nextjs에서 운영환경에서 설치해야함)
```
